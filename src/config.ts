import type { GridCompanyId } from "@features/electricity-prices";

/**
 * Set this to the ID of your net company (DSO).
 * Valid values: "radius" | "cerius" | "n1" | "trefor" | "konstant" | "dinel"
 */
export const MY_GRID_COMPANY: GridCompanyId = "dinel";
